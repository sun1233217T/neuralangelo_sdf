# conda env create --file neuralangelo.yaml
name: neuralangelo
channels:
  - conda-forge
dependencies:
  # general
  - gpustat
  - gdown
  - cuda
  - cmake
  # python general
  - python=3.10
  - pip
  - numpy
  - scipy
  - ipython
  - jupyterlab
  - cython
  - ninja
  - diskcache
  - pip:
    - -r requirements.txt
